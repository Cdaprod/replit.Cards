const card = document.querySelector('card');

constructor() {
      document.collection.map((card) => {
        1. { name: David};
        2. { name: Kayson};
        3. { name: Kayne};
        4. { name: Kole};
      });
   }

const card();

